# Dev profile — verbose errors OK locally
# Activate with: SPRING_PROFILES_ACTIVE=dev  (or omit — base file already allows INFO)

# Full error detail during local development
server.error.include-message=always
server.error.include-exception=true
server.error.include-stacktrace=always

# Verbose logging during development
logging.level.com.pivot.agent=DEBUG
